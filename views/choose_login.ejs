<!DOCTYPE html>
<html>
<head>
    <title>Hostel Management - Choose Login</title>
    <link rel="stylesheet" href="/css/style.css">
        <style>
            body {
                font-family: Arial, sans-serif;
                background: linear-gradient(to right, #6a11cb, #2575fc);
                color: #fff;
                text-align: center;
                height: 100vh;
                display: flex;
                flex-direction: column;
                justify-content: center;
                align-items: center;
                margin: 0;
            }

            h2 {
                font-size: 2.5em;
                margin-bottom: 40px;
                text-shadow: 2px 2px 5px rgba(0,0,0,0.3);
            }

            .login-options {
                display: flex;
                flex-wrap: wrap;
                justify-content: center;
                gap: 20px;
            }

            .login-btn {
                display: inline-block;
                padding: 15px 30px;
                font-size: 1.2em;
                background-color: #ff6b6b;
                color: white;
                text-decoration: none;
                border-radius: 8px;
                box-shadow: 0 5px 15px rgba(0,0,0,0.2);
                transition: all 0.3s ease;
            }

            .login-btn:hover {
                background-color: #ff4757;
                transform: translateY(-3px);
                box-shadow: 0 8px 20px rgba(0,0,0,0.3);
            }

            @media (max-width: 500px) {
                .login-btn {
                    width: 80%;
                    font-size: 1em;
                }
            }
        </style>
</head>
<body>
    <h2>Login as:</h2>
    <div class="login-options">
        <a href="/login/student" class="login-btn">Student</a>
        <a href="/login/warden" class="login-btn">Warden</a>
        <a href="/login/admin" class="login-btn">Admin</a>
        <a href="/login/security" class="login-btn">Security</a>
    </div>
</body>
</html>
