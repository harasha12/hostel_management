<!DOCTYPE html>
<html>
<head>
  <title>Verify Student Fees</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body {
      background: #f6f7fb;
      font-family: 'Poppins', 'Segoe UI', sans-serif;
      margin: 0;
      padding: 0;
      color: #1b2430;
    }

    .container {
      max-width: 1050px;
      margin: 60px auto 40px auto;
      background: #fff;
      padding: 35px 40px;
      border-radius: 16px;
      box-shadow: 0 10px 35px #22304c22, 0 2px 6px #00000015;
      border: 3px solid #ffd700;
    }

    h2 {
      text-align: center;
      font-weight: 800;
      font-size: 1.9em;
      color: #222;
      letter-spacing: 0.8px;
      border-bottom: 3px solid #ffd700;
      display: inline-block;
      margin-bottom: 25px;
      padding-bottom: 6px;
      background: linear-gradient(90deg, #8E8EED 60%, #EBD483 100%);
      border-radius: 6px;
      padding: 8px 16px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 15px;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 2px 10px #00000010;
    }

    table th {
      background: linear-gradient(135deg, #f7d358, #f0b429);
      color: #222;
      padding: 14px;
      font-weight: 700;
      font-size: 1em;
      letter-spacing: 0.4px;
      border-bottom: 2px solid #ddd;
    }

    table td {
      padding: 12px 14px;
      border-bottom: 1px solid #eee;
      background: #fff;
      text-align: center;
      font-size: 0.97em;
      color: #333;
      vertical-align: middle;
    }

    table tr:nth-child(even) td {
      background: #fafafa;
    }

    table a {
      color: #007bff;
      text-decoration: none;
      font-weight: 600;
      transition: 0.2s;
    }

    table a:hover {
      color: #0056b3;
      text-decoration: underline;
    }

    /* Buttons */
    .btn-approve, .btn-reject {
      padding: 8px 16px;
      border-radius: 50px;
      text-decoration: none;
      font-weight: 600;
      color: #fff;
      border: none;
      cursor: pointer;
      transition: 0.25s ease;
      font-size: 0.95em;
      letter-spacing: 0.4px;
      box-shadow: 0 2px 8px #00000022;
    }

    .btn-approve {
      background: linear-gradient(135deg, #28a745, #34d058);
    }

    .btn-approve:hover {
      background: linear-gradient(135deg, #34d058, #28a745);
      transform: scale(1.05);
      box-shadow: 0 0 12px #28a74555;
    }

    .btn-reject {
      background: linear-gradient(135deg, #dc3545, #ff6b6b);
    }

    .btn-reject:hover {
      background: linear-gradient(135deg, #ff6b6b, #dc3545);
      transform: scale(1.05);
      box-shadow: 0 0 12px #ff6b6b66;
    }

    /* Back Link */
    .back-link {
      display: block;
      margin: 30px auto 0 auto;
      text-align: center;
      font-weight: 700;
      color: #fff;
      text-decoration: none;
      font-size: 1.05em;
      width: 60%;
      max-width: 340px;
      padding: 12px 0;
      border-radius: 50px;
      background: linear-gradient(90deg, #d4af37, #ffdd00);
      box-shadow: 0 4px 12px #d4af3744;
      transition: all 0.25s ease;
      letter-spacing: 0.7px;
    }

    .back-link:hover {
      background: linear-gradient(90deg, #ffdd00, #ffe382);
      color: #1b243b;
      transform: scale(1.06);
      box-shadow: 0 0 20px #ffe082a9;
    }

    @media (max-width: 768px) {
      .container {
        padding: 20px;
        margin: 40px 16px;
      }
      h2 {
        font-size: 1.4em;
      }
      table th, table td {
        font-size: 0.9em;
        padding: 10px;
      }
      .btn-approve, .btn-reject {
        padding: 6px 12px;
        font-size: 0.85em;
      }
    }
    .status-badge {
  display: inline-block;
  padding: 6px 15px;
  border-radius: 50px;
  font-weight: 700;
  font-size: 0.95em;
  letter-spacing: 0.3px;
  min-width: 90px;
  text-align: center;
}
.status-badge.approved {
  background: linear-gradient(90deg,#c7f7e1 0%,#6edea8 100%); /* Light Green to Green */
  color: #159469; /* Darker Green text */
  border: 1.5px solid #6edea8;
}
.status-badge.rejected {
  background: linear-gradient(90deg,#ffe9e9 0%,#ffc0c0 100%); /* Light Red to Pink */
  color: #d93232; /* Dark Red text */
  border: 1.5px solid #ffc0c0;
}

  </style>
</head>
<body>
  <%- include('../partials/header') %>

  <div class="container">
    <h2>Verify Student Fee Receipts</h2>

    <table>
      <tr>
        <th>Student ID</th>
        <th>Name</th>
        <th>Course</th>
        <th>Year</th>
        <th>Receipt</th>
        <th>Status</th>
        <th>Action</th>
      </tr>
      <% receipts.forEach(r => { %>
        <tr>
          <td><%= r.student_id %></td>
          <td><%= r.name %></td>
          <td><%= r.course %></td>
          <td><%= r.year %></td>
          <td>
            <% if (r.pdf_path) { %>
              <a href="/uploads/receipts/<%= r.pdf_path %>" target="_blank">View</a>
           <% receipts.forEach(r => { %>
        <tr>
          <td><%= r.student_id %></td>
          <td><%= r.name %></td>
          <td><%= r.course %></td>
          <td><%= r.year %></td>
          <td>
            <% if (r.pdf_path) { %>
              <a href="/uploads/receipts/<%= r.pdf_path %>" target="_blank">View</a>
            <% } else { %>
              N/A
            <% } %>
          </td>
                    <td>
            <% if (r.status === 'Approved' || r.status === 'Verified') { %>
              <span class="status-badge approved"><%= r.status %></span>
            <% } else if (r.status === 'Rejected') { %>
              <span class="status-badge rejected"><%= r.status %></span>
            <% } else { %>
              <span><%= r.status %></span>
            <% } %>
          </td>
          <td>
  <% if (r.status === 'Pending') { %>
    <form action="/admin/verifyFees/<%= r.receipt_id %>" method="POST" style="display:inline;">
      <input type="hidden" name="status" value="Verified">
      <button type="submit" class="btn-approve">Approve</button>
    </form>
    <form action="/admin/verifyFees/<%= r.receipt_id %>" method="POST" style="display:inline;">
      <input type="hidden" name="status" value="Rejected">
      <button type="submit" class="btn-reject">Reject</button>
    </form>
  <% } else if (r.status === 'Approved' || r.status === 'Verified') { %>
    <span class="status-badge approved"><%= r.status %></span>
  <% } else if (r.status === 'Rejected') { %>
    <span class="status-badge rejected"><%= r.status %></span>
  <% } else { %>
    <span><%= r.status %></span>
  <% } %>
</td>

        </tr>
      <% }) %>
    </table>

    <a href="/admin/dashboard" class="back-link">⬅ Back to Dashboard</a>
  </div>

  <%- include('../partials/footer') %>
</body>
</html>