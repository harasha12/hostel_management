<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Emergency Outpass | Student Portal</title>
    <style>
        /* ========= DESIGN TOKENS ========= */
        :root {
            --color-bg-main: #141927;
            --color-bg-surface: #1f2937;
            --color-bg-surface-soft: #111827;
            --color-text-main: #e5e7eb;
            --color-text-muted: #9ca3af;
            --color-accent-gold: #fbbf24;
            --color-accent-soft: rgba(251, 191, 36, 0.14);

            --color-emergency: #ef4444;
            --color-emergency-hover: #dc2626;

            --color-input-bg: #111827;
            --color-input-border: #374151;

            --color-table-header: #020617;
            --color-table-row-alt: #111827;
            --color-table-border: #1f2937;

            --status-approved: #22c55e;
            --status-pending: #facc15;
            --status-rejected: #ef4444;

            --radius-lg: 14px;
            --radius-md: 10px;
            --shadow-soft: 0 18px 45px rgba(15, 23, 42, 0.55);
            --shadow-subtle: 0 10px 30px rgba(15, 23, 42, 0.35);
        }

        /* ========= GLOBAL ========= */
        * {
            box-sizing: border-box;
        }

      body {
    margin: 0;
    padding: 40px 16px 32px;
    min-height: 100vh;
    font-family: "Poppins", sans-serif;

    background:
        linear-gradient(rgba(0,0,0,0.70), rgba(0,0,0,0.70)),
        url('/images/hostel-bg.jpg') no-repeat center center fixed;

    background-size: cover;
    color: var(--color-text-main);
}

        .container {
            max-width: 1000px;
            margin: 0 auto;
        }

        /* ========= HEADER ========= */
        h2{
    color:#ffd700;
    text-shadow:2px 2px 8px rgba(0,0,0,.6);
}

.page-subtitle{
    text-align:center;
    font-size:14px;
    color:#ffffff;
    margin-bottom:28px;
}

h3 {
    margin: 40px 0 14px;
    font-size: 18px;
    font-weight: 600;
    color: #ffffff;
    display: inline-flex;
    align-items: center;
    gap: 8px;
}

        h3::after {
            content: "";
            display: inline-block;
            width: 56px;
            height: 2px;
            border-radius: 999px;
            background: linear-gradient(90deg, var(--color-accent-gold), transparent);
        }

        /* ========= FORM CARD ========= */
        .outpass-form {
    background: rgba(255,255,255,0.96);
    backdrop-filter: blur(8px);
    border-radius: 16px;
    padding: 24px 22px 26px;
    box-shadow: 0 15px 40px rgba(0,0,0,0.35);
    border: 2px solid #ffd700;
    max-width: 520px;
    margin: 0 auto 40px;
}

        .form-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 8px;
            margin-bottom: 18px;
        }

        .form-title {
    font-size: 18px;
    font-weight: 700;
    color: #22304c;
}

        .form-chip {
            font-size: 11px;
            padding: 3px 10px;
            border-radius: 999px;
            background: var(--color-accent-soft);
            color: var(--color-accent-gold);
            border: 1px solid rgba(251, 191, 36, 0.6);
            text-transform: uppercase;
            letter-spacing: 0.08em;
        }

       label{
    color:#22304c;
    font-weight:700;
}

       textarea,
input[type="date"] {
    width: 100%;
    padding: 10px 12px;
    border: 2px solid #cbd5e1;
    border-radius: 10px;
    background: #ffffff;
    color: #22304c;
    font-size: 15px;
}

textarea:focus,
input[type="date"]:focus{
    background:#fff;
    border-color:#ffc107;
    box-shadow:0 0 8px rgba(255,193,7,.35);
}

        textarea {
            resize: vertical;
            min-height: 80px;
            max-height: 180px;
        }

        textarea::placeholder {
    color: #64748b;
}

        textarea:focus,
input[type="date"]:focus {
    background: #ffffff;
    color: #22304c;
    border: 2px solid #ffc107;
    box-shadow: 0 0 8px rgba(255,193,7,0.35);
    outline: none;
}

        .form-row {
            display: flex;
            gap: 20px;
            margin-bottom: 4px;
        }

        .form-col {
            flex: 1;
        }

        /* ========= BUTTON ========= */
        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 100%;
            margin-top: 16px;
            padding: 11px 14px;
            border-radius: var(--radius-md);
            border: none;
            cursor: pointer;
            font-size: 15px;
            font-weight: 600;
            letter-spacing: 0.04em;
            text-transform: uppercase;
            background: linear-gradient(135deg, var(--color-emergency), var(--color-emergency-hover));
            color: #fef2f2;
            box-shadow: 0 14px 35px rgba(239, 68, 68, 0.6);
            transition:
                transform 0.08s ease,
                box-shadow 0.2s ease,
                filter 0.15s ease;
        }

        .btn:hover {
            transform: translateY(-1px);
            filter: brightness(1.03);
            box-shadow: 0 18px 45px rgba(239, 68, 68, 0.75);
        }

        .btn:active {
            transform: translateY(0);
            box-shadow: 0 8px 22px rgba(239, 68, 68, 0.6);
        }

        /* ========= TABLE CARD ========= */
        .table-card {
    margin-top: 8px;
    border-radius: 16px;
    background: rgba(255,255,255,0.96);
    backdrop-filter: blur(8px);
    box-shadow: 0 15px 40px rgba(0,0,0,0.35);
    border: 2px solid #ffd700;
    padding: 20px;
}

        .table-header{
    color:#22304c;
}

table.outpasses-table{
    width:100%;
    border-collapse:collapse;
    font-size:13px;
    background:#ffffff;
}

.outpasses-table th{
    background:#22304c;
    color:#ffd700;
}

.outpasses-table td{
    color:#22304c;
}

        .table-count-pill {
            padding: 3px 9px;
            border-radius: 999px;
            background: rgba(31, 41, 55, 0.9);
            border: 1px solid rgba(55, 65, 81, 0.9);
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 0.08em;
        }

        .table-wrapper {
            width: 100%;
            overflow-x: auto;
            border-radius: 10px;
            border: 1px solid var(--color-table-border);
        }

       table.outpasses-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 13px;
    background: #ffffff;
}

        .outpasses-table th,
        .outpasses-table td {
            padding: 10px 12px;
            text-align: left;
            border-bottom: 1px solid rgba(31, 41, 55, 0.9);
            white-space: nowrap;
        }

        .outpasses-table th {
    background: #22304c;
    color: #ffd700;
    font-size: 12px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: .08em;
}
      .outpasses-table tbody tr:nth-child(even){
    background:#f8fafc;
}

.outpasses-table tbody tr:hover{
    background:#fff9db;
}
        .outpasses-table tbody tr:hover {
            
            transition: background-color 0.18s ease;
        }

        .outpasses-table td:first-child,
        .outpasses-table th:first-child {
            padding-left: 16px;
        }

        .outpasses-table td:last-child,
        .outpasses-table th:last-child {
            padding-right: 16px;
        }

        .reason-cell {
            max-width: 260px;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        /* ========= STATUS BADGES ========= */
        .status-badge {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            padding: 4px 10px;
            border-radius: 999px;
            font-weight: 600;
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 0.08em;
        }

        .status-dot {
            width: 8px;
            height: 8px;
            border-radius: 999px;
            background: currentColor;
        }

        .status-APPROVED {
            background-color: rgba(34, 197, 94, 0.18);
            color: var(--status-approved);
        }

        .status-PENDING {
            background-color: rgba(250, 204, 21, 0.2);
            color: var(--status-pending);
        }

        .status-REJECTED {
            background-color: rgba(239, 68, 68, 0.2);
            color: var(--status-rejected);
        }

        /* ========= EMPTY STATE ========= */
        .empty-row {
            text-align: center;
            padding: 18px 0;
            color: var(--color-text-muted);
        }

        /* ========= RESPONSIVE ========= */
        @media (max-width: 700px) {
            body {
                padding-top: 26px;
            }

            h2 {
                font-size: 22px;
            }

            .outpass-form {
                padding: 20px 16px 22px;
            }

            .form-row {
                flex-direction: column;
                gap: 12px;
            }

            .table-card {
                padding: 12px 8px 10px;
            }

            .table-wrapper {
                border-radius: 0;
                border: none;
            }

            /* card-style rows on mobile */
            table.outpasses-table {
                border-collapse: separate;
                border-spacing: 0 10px;
                background: transparent;
            }

            .outpasses-table thead {
                display: none;
            }

            .outpasses-table tbody tr {
                display: block;
                background: var(--color-bg-surface-soft);
                border-radius: 12px;
                border: 1px solid rgba(31, 41, 55, 0.9);
                padding: 8px 10px;
                margin: 0 4px 10px;
            }

            .outpasses-table td {
                display: flex;
                justify-content: space-between;
                align-items: center;
                border-bottom: none;
                padding: 6px 4px;
                white-space: normal;
                font-size: 12px;
            }

            .outpasses-table td::before {
                content: attr(data-label);
                font-weight: 600;
                color: var(--color-text-muted);
                margin-right: 10px;
                text-transform: uppercase;
                letter-spacing: 0.08em;
                font-size: 11px;
            }

            .reason-cell {
                max-width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Emergency Outpass</h2>
        <div class="page-subtitle">
            Use this form only for genuine emergencies. All requests are logged and reviewed by hostel authorities.
        </div>

        <form action="/student/applyEmergencyOutpass" method="POST" class="outpass-form">
            <div class="form-header">
                <div class="form-title">Emergency Outpass Application</div>
                <div class="form-chip">High Priority</div>
            </div>

            <div style="margin-bottom: 16px;">
                <label for="reason">Reason</label>
                <textarea id="reason" name="reason" placeholder="Describe your emergency briefly and clearly" required></textarea>
            </div>

            <div class="form-row">
                <div class="form-col">
                    <label for="from_date">Out Date</label>
                    <input type="date" id="from_date" name="from_date" required>
                </div>
                <div class="form-col">
                    <label for="to_date">Return Date</label>
                    <input type="date" id="to_date" name="to_date" required>
                </div>
            </div>

            <button type="submit" class="btn">
                Apply Emergency Outpass
            </button>
        </form>

        <h3>Your Emergency Requests</h3>
        <div class="table-card">
            <div class="table-header">
                <span>History of submitted emergency outpasses</span>
                <span class="table-count-pill">
                    TOTAL: <%= outpasses.length || 0 %>
                </span>
            </div>

            <div class="table-wrapper">
                <table class="outpasses-table">
                    <thead>
                        <tr>
                            <th>Reason</th>
                            <th>Out Date</th>
                            <th>Return Date</th>
                            <th>Status</th>
                            <th>Applied On</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% outpasses.forEach(o => { 
                            const status = o.status ? o.status.toUpperCase() : 'PENDING';
                        %>
                        <tr>
                            <td class="reason-cell" data-label="Reason" title="<%= o.reason %>"><%= o.reason %></td>
                            <td data-label="Out Date"><%= new Date(o.out_date).toISOString().split('T')[0] %></td>
                            <td data-label="Return Date"><%= new Date(o.return_date).toISOString().split('T')[0] %></td>
                            <td data-label="Status">
                                <span class="status-badge status-<%= status %>">
                                    <span class="status-dot"></span>
                                    <%= status %>
                                </span>
                            </td>
                            <td data-label="Applied On">
                                <%= new Date(o.created_at).toLocaleString('en-IN', { dateStyle: 'short', timeStyle: 'short' }) %>
                            </td>
                        </tr>
                        <% }) %>

                        <% if (outpasses.length === 0) { %>
                            <tr>
                                <td colspan="5" class="empty-row">
                                    No emergency outpass requests found.
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    
</body>
</html>
