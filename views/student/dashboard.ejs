<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>CRR Boys Hostel | Student Dashboard</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

  <style>
    /* ========== GENERAL ========== */
    body {
      min-height: 100vh;
      margin: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(rgba(0,0,0,0.7), rgba(0,0,0,0.7)),
                  url('/images/hostel-bg.jpg') no-repeat center center fixed;
      background-size: cover;
      color: #fff;
      overflow-x: hidden;
    }

    /* ========== HEADER ========== */
    /* ========== HEADER ========== */
.college-header {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 25px 20px;

  /* ✨ Gold + Sand + BlackSand Gradient */
  background: linear-gradient(90deg, #0a1a3f 0%, #153b6f 60%, #1a1a1a 100%);


  border-bottom: 3px solid rgba(0, 0, 0, 0.4);
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.6);
  backdrop-filter: blur(5px);
}

/* Optional: make text more readable */
.college-header .header-text h2,
.college-header .header-text p {
  color: #fff;
  text-shadow: 2px 2px 5px #000;
}

.college-header img {
  max-height: 80px;
  margin-right: 15px;
  filter: drop-shadow(0 0 6px rgba(0, 0, 0, 0.7));
}


    .college-header img {
    max-height: 100px;
    margin-right: 20px;
    filter: drop-shadow(0 0 5px rgba(255, 255, 255, 0.5));
    animation: float 3s ease-in-out infinite;
}

/* Floating animation */
@keyframes float {
    0%   { transform: translateY(0px); }
    50%  { transform: translateY(-10px); } /* moves up 10px */
    100% { transform: translateY(0px); }
}

    .college-header .header-text h2 {
      margin: 0;
      font-size: 1.9rem;
      font-weight: bold;
      color: #fff;
      text-transform: uppercase;
      text-shadow: 2px 2px 4px #000;
    }

    .college-header .header-text p {
      margin: 6px 0 0;
      font-size: 0.9rem;
      color: #f0f0f0;
      line-height: 1.4;
    }

    .college-header .autonomous {
      color: #ffcc00;
      font-weight: bold;
      text-shadow: 1px 1px 2px #000;
    }

    /* ---------------- NOTIFICATION BAR ---------------- */
    .notification-banner {
  position: fixed;
  top: 160px;
  left: 0;
  width: 100%;
  background-color: #FFD700;
  color: #222;
  padding: 8px 0;
  font-weight: bold;
  z-index: 997;
  overflow: hidden;
}

.scrolling-text {
  display: inline-block;
  white-space: nowrap;
  padding-left: 100%;
  animation: scroll-left 15s linear infinite;
}

@keyframes scroll-left {
  from { transform: translateX(0%); }
  to { transform: translateX(-100%); }
}

    /* ========== SIDEBAR ========== */
    .sidebar {
      position: fixed;
      top: 150px;
      left: 0;
      height: calc(100vh - 150px);
      width: 230px;
      background: linear-gradient(180deg, #0d1b4c, #12306b);
      box-shadow: 4px 0 10px rgba(0,0,0,0.4);
      transition: width 0.3s ease;
      display: flex;
      z-index: 998;
       background-color: #111;
      flex-direction: column;
      overflow: hidden;
    }

    .sidebar.collapsed {
      width: 70px;
    }
    

    /* ========== TOGGLE BUTTON ========== */
    .toggle-btn {
      position: fixed;
      top: 160px;
      left: 15px;
      background: #0d1b4c;
      border-radius: 50%;
      width: 45px;
      height: 45px;
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      box-shadow: 0 0 10px rgba(0,0,0,0.5);
      transition: transform 0.3s, background 0.3s;
      z-index: 1200;
    }

    .toggle-btn.active {
      background: #FFD700;
    }

    .toggle-btn.active i {
      color: #0d1b4c;
    }

    .toggle-btn:hover {
      transform: rotate(90deg) scale(1.1);
    }

    .toggle-btn i {
      color: #FFD700;
      font-size: 1.4em;
      transition: color 0.3s;
    }

    /* ========== NAV LINKS ========== */
    .nav-links {
      list-style: none;
      padding: 60px 0 0;
      margin: 0;
      flex-grow: 1;
      overflow-y: auto;
    }

    .nav-links li {
      margin: 10px0;
    }

    .nav-links a {
      display: flex;
      align-items: center;
      padding: 10px 18px;
      color: #fff;
      text-decoration: none;
      font-size: 0.95em;
      font-weight: 600;
      transition: all 0.3s ease;
    }

    .nav-links a i {
      font-size: 1.3em;
      color: #FFD700;
      width: 30px;
      text-align: center;
      transition: transform 0.3s;
    }

    .nav-links a span {
      transition: opacity 0.3s;
    }

    .sidebar.collapsed a span {
      opacity: 0;
      pointer-events: none;
    }

    .nav-links a:hover {
      background: rgba(255, 215, 0, 0.2);
      transform: translateX(4px);
    }

    .nav-links a.active {
      background: rgba(255, 215, 0, 0.3);
      border-left: 4px solid #FFD700;
    }

    /* ========== LOGOUT SECTION ========== */
    .logout-section {
      width: 100%;
      padding: 20px 0;
      text-align: center;
      margin-top: auto;
    }

    .logout-separator {
      width: 75%;
      height: 1px;
      margin: 0 auto 10px auto;
      background: #FFD700;
      opacity: 0.5;
    }

    .logout-btn {
      background: transparent;
      border: 2px solid #FFD700;
      color: #FFD700;
      border-radius: 8px;
      padding: 10px 16px;
      font-weight: 600;
      cursor: pointer;
      transition: 0.3s;
    }

    .logout-btn:hover {
      background: #FFD700;
      color: #0d1b4c;
      transform: scale(1.05);
    }

    /* ========== MAIN CONTENT ========== */
    .page-content {
      margin-top: 230px;
      margin-left: 250px;
      padding: 20px;
      transition: margin-left 0.3s ease;
    }

    .sidebar.collapsed ~ .page-content {
      margin-left: 90px;
    }

    h2 {
      text-align: center;
      color: #f9d342;
      text-shadow: 2px 2px 6px #000;
    }

    .card {
      background: rgba(255,255,255,0.1);
      border-radius: 15px;
      padding: 25px;
      margin-bottom: 30px;
      backdrop-filter: blur(5px);
      box-shadow: 0 4px 20px rgba(0,0,0,0.5);
      transition: 0.3s;
    }

    .card:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 25px rgba(0,0,0,0.6);
    }

    .card h3 {
      color: #FFD700;
      text-align: center;
      margin-bottom: 15px;
      text-decoration: underline;
    }
    /* ===== Profile Card Layout ===== */
.profile-card {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.profile-content {
  display: flex;
  align-items: center;
  justify-content: flex-start;
  gap: 25px;
  width: 100%;
  flex-wrap: wrap;
}

.profile-photo img {
  width: 200px;
  height: auto;
  border-radius: 10%;
  object-fit: cover;
  border: 3px solid #FFD700;
  box-shadow: 0 0 10px rgba(255, 215, 0, 0.6);
}

.profile-details {
  flex: 1;
  font-size: 1rem;
  color: #fff;
  line-height: 1.6;
}

.profile-details p {
  margin: 6px 0;
}


    .fee-list {
      list-style: none;
      padding: 0;
    }

    .fee-list li {
      background: rgba(255,255,255,0.15);
      border-radius: 8px;
      padding: 10px;
      margin: 6px 0;
      display: flex;
      justify-content: space-between;
      font-weight: bold;
    }

    /* ========== RESPONSIVE ========== */
    @media (max-width: 768px) {
      .college-header img { max-height: 60px; }
      .sidebar { width: 200px; }
      .sidebar.collapsed { width: 65px; }
      .page-content { margin-left: 210px; }
      .sidebar.collapsed ~ .page-content { margin-left: 80px; }
    }
  </style>

  <script>
    function toggleSidebar() {
      const sidebar = document.querySelector('.sidebar');
      const toggleBtn = document.querySelector('.toggle-btn');
      sidebar.classList.toggle('collapsed');
      toggleBtn.classList.toggle('active');
    }

    function loadPage(page, btn) {
      document.querySelectorAll('.nav-links a').forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      fetch('/student/partial/' + page)
        .then(res => res.text())
        .then(html => {
          document.getElementById('mainContent').innerHTML = html;
        })
        .catch(() => {
          document.getElementById('mainContent').innerHTML = "<p>⚠ Page not found.</p>";
        });
    }
  </script>
</head>

<body>
  <!-- College Header -->
  <div class="college-header">
    <img src="/images/college_logo.png" alt="College Logo" />
    <div class="header-text">
      <h2>SIR C R REDDY COLLEGE OF ENGINEERING - BOYS HOSTEL (<span class="autonomous">AUTONOMOUS</span>)</h2>
      <p>Approved by AICTE & Permanently Affiliated to JNTUk, Kakinada, NAAC-A<br>
      Accredited by NBA (CSE, IT, EEE, Mech, ECE, Civil)<br>
      Vatluru, Eluru, Eluru District, AP - 534007</p>
    </div>
  </div>

  <!-- 🔔 Notification Bar -->
  <div class="notification-banner">
    <div class="scrolling-text">
      ⚠ Important Notice: Hostel gate will close at 9:00 PM daily | Submit your fee receipts before 5th Nov | Contact warden for any issues.
    </div>
  </div>

  <!-- Sidebar Toggle Button -->
  <div class="toggle-btn" onclick="toggleSidebar()">
    <i class="fa-solid fa-bars"></i>
  </div>

  <!-- Sidebar -->
  <div class="sidebar">
    <ul class="nav-links">
      <li><a href="/student/profile" class="active"><i class="fa-regular fa-user"></i><span>Profile</span></a></li>
      <li><a href="/student/applyoutpass"><i class="fa-regular fa-id-card"></i><span>Apply Outpass</span></a></li>
      <li><a href="/student/viewattendance"><i class="fa-regular fa-calendar-check"></i><span>View Attendance</span></a></li>
      <li><a href="/student/outpasses"><i class="fa-regular fa-file-lines"></i><span>My Outpasses</span></a></li>
      <li><a href="/student/applyEmergencyOutpass"><i class="fa-regular fa-file-lines"></i><span>Apply Emergency Outpasses</span></a></li>
      <li><a href="/student/viewfees"><i class="fa-solid fa-indian-rupee-sign"></i><span>View Fees</span></a></li>
      <li><a href="/student/uploadReceipt"><i class="fa-solid fa-cloud-arrow-up"></i><span>Upload Receipt</span></a></li>
      <li><a href="/student/complaint"><i class="fa-regular fa-comment-dots"></i><span>Complaint</span></a></li>
      <li><a href="/student/viewComplaints"><i class="fa-regular fa-folder-open"></i><span>My Complaints</span></a></li>
    </ul>

    <div class="logout-section">
      <div class="logout-separator"></div>
      <button class="logout-btn" onclick="location.href='/logout'">
        <i class="fa-solid fa-arrow-right-from-bracket"></i> Logout
      </button>
    </div>
  </div>

  <!-- Main Page Content -->
  <div class="page-content" id="mainContent">
    <h2>Welcome, <%= student.name %>!</h2>

    <div class="card profile-card">
  <h3>Profile Information</h3>
  <div class="profile-content">
    <div class="profile-photo">
 <img src="<%= student.profile_image ? '/uploads/profile_images/' + student.profile_image : '/images/profile.jpg' %>" alt="Student Photo">



    </div>
    <div class="profile-details">
      <p><strong>Student ID:</strong> <%= student.student_id %></p>
      <p><strong>Name:</strong> <%= student.name %></p>
      <p><strong>Email:</strong> <%= student.email %></p>
      <p><strong>Room:</strong> <%= student.room_no || "Not Assigned" %></p>
      <p><strong>Course:</strong> <%= student.course || "N/A" %> |
         <strong>Year:</strong> <%= student.year || "N/A" %></p>
    </div>
  </div>
</div>

  </div>
</body>
</html>