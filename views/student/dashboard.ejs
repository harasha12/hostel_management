<!DOCTYPE html>
<html>
<head>
    <title>Student Dashboard</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background: url("/images/bg.jpg") no-repeat center center fixed;
            background-size: cover;
            margin: 0;
            padding: 0;
            text-align: center;
        }

        h2 {
            color: #fff;
            margin-top: 30px;
        }

        .profile-card {
            background: rgba(0,0,0,0.7);
            color: #fff;
            padding: 20px;
            margin: 20px auto;
            border-radius: 12px;
            max-width: 400px;
            box-shadow: 0px 4px 10px rgba(0,0,0,0.5);
        }

        .profile-card p {
            margin: 10px 0;
        }

        .actions {
            margin: 30px auto;
            max-width: 400px;
        }

        .actions a, .actions button {
            display: block;
            background: #007BFF;
            color: white;
            padding: 12px;
            margin: 10px 0;
            border-radius: 8px;
            text-decoration: none;
            font-weight: bold;
            transition: 0.3s;
            border: none;
            cursor: pointer;
        }

        .actions a:hover, .actions button:hover {
            background: #0056b3;
        }

        form {
            margin: 0;
        }
    </style>
</head>
<body>
    <h2>Welcome, <%= user.name %>!</h2>

    <div class="profile-card">
        <h3>Profile Info</h3>
        <p><strong>Student ID:</strong> <%= user.student_id %></p>
        <p><strong>Email:</strong> <%= user.email %></p>
        <p><strong>Room:</strong> <%= user.room_no || "Not Assigned" %></p>
        <p><strong>Course:</strong> <%= user.course || "N/A" %> | <strong>Year:</strong> <%= user.year || "N/A" %></p>
    </div>

    <h3>Actions</h3>
<ul>
    <li><a href="/student/profile">Update Profile</a></li>
    <li><a href="/student/applyoutpass">Apply for Outpass</a></li>
    <li><a href="/student/viewattendance">View Attendance</a></li>
    <li><a href="/student/viewfees">View Fees</a></li>
    <li><a href="/student/uploadReceipt">Upload Receipt</a></li>
    <li>
        <form action="/logout" method="POST">
            <button type="submit">Logout</button>
        </form>
    </li>
</ul>

</body>
</html>
