<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Submit Complaint | Student Portal</title>

    <style>
        /* ========= DESIGN TOKENS ========= */
        :root {
            --bg-dark: #020617;
            --bg-surface: rgba(15, 23, 42, 0.92);
            --bg-card: rgba(15, 23, 42, 0.85);

            --text-main: #e5e7eb;
            --text-muted: #9ca3af;

            --gold: #fbbf24;

            --input-bg: rgba(15, 23, 42, 0.9);
            --input-border: #374151;

            --btn-bg: #6366f1;
            --btn-bg-hover: #4f46e5;

            --radius-lg: 16px;
            --radius-md: 10px;

            --shadow-soft: 0 20px 45px rgba(0, 0, 0, 0.55);
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            padding: 38px 16px;
            font-family: "Poppins", system-ui, sans-serif;
            background: radial-gradient(circle at top, #1f2937 0, #020617 55%);
            color: var(--text-main);
            min-height: 100vh;
        }

        .container {
            max-width: 650px;
            margin: 0 auto;
        }

        /* ========= PAGE HEADER ========= */
        h2 {
            text-align: center;
            font-size: 26px;
            margin: 0 0 8px;
            color: var(--gold);
            text-transform: uppercase;
            letter-spacing: 1px;
            font-weight: 700;
        }

        .subtitle {
            text-align: center;
            font-size: 13px;
            color: var(--text-muted);
            margin-bottom: 26px;
        }

        /* ========= FORM CARD ========= */
        .form-card {
            background: var(--bg-card);
            padding: 26px 24px 28px;
            border-radius: var(--radius-lg);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(148,163,184,0.25);
            box-shadow: var(--shadow-soft);
        }

        label {
            display: block;
            margin-bottom: 6px;
            margin-top: 18px;
            font-size: 13px;
            font-weight: 500;
            color: var(--text-muted);
            letter-spacing: 0.05em;
            text-transform: uppercase;
        }

        input[type="text"],
        textarea {
            width: 100%;
            padding: 11px 12px;
            border-radius: var(--radius-md);
            border: 1px solid var(--input-border);
            background: var(--input-bg);
            color: var(--text-main);
            font-size: 14px;
            outline: none;
            transition: 0.25s ease;
        }

        textarea {
            resize: vertical;
            min-height: 110px;
        }

        input:focus,
        textarea:focus {
            border-color: var(--gold);
            box-shadow: 0 0 0 1px rgba(251,191,36,0.45);
            background: #020617;
        }

        /* ========= BUTTON ========= */
        .btn-submit {
            width: 100%;
            margin-top: 26px;
            padding: 12px;
            border-radius: var(--radius-md);
            background: linear-gradient(135deg, var(--btn-bg), var(--btn-bg-hover));
            color: #eef2ff;
            font-size: 15px;
            font-weight: 700;
            text-transform: uppercase;
            border: none;
            cursor: pointer;
            box-shadow: 0 14px 30px rgba(99,102,241,0.4);
            transition: 0.2s ease;
        }

        .btn-submit:hover {
            transform: translateY(-2px);
            filter: brightness(1.05);
            box-shadow: 0 18px 38px rgba(99,102,241,0.55);
        }

        .btn-submit:active {
            transform: translateY(0);
            box-shadow: 0 8px 22px rgba(99,102,241,0.4);
        }

        /* ========= BACK LINK ========= */
        .back-link {
            display: block;
            margin: 26px auto 0;
            width: 250px;
            text-align: center;
            padding: 11px;
            border-radius: var(--radius-md);
            text-decoration: none;
            font-weight: 700;
            background: var(--gold);
            color: #111827;
            letter-spacing: 0.06em;
            box-shadow: 0 12px 26px rgba(251,191,36,0.28);
            transition: 0.2s ease;
        }

        .back-link:hover {
            background: #ffdd4d;
            transform: translateY(-2px);
        }

        /* ========= RESPONSIVE ========= */
        @media (max-width: 700px) {
            h2 { font-size: 22px; }

            .form-card {
                padding: 20px 16px 22px;
            }

            .back-link {
                width: 90%;
            }
        }
    </style>
</head>

<body>

<div class="container">
    <h2>Submit Complaint</h2>
    <div class="subtitle">Submit hostel-related complaints. Warden will review and respond soon.</div>

    <div class="form-card">
        <form action="/student/complaint" method="POST">
            <label>Subject</label>
            <input type="text" name="subject" required placeholder="Enter complaint subject">

            <label>Description</label>
            <textarea name="description" required placeholder="Describe your issue clearly"></textarea>

            <button type="submit" class="btn-submit">Submit Complaint</button>
        </form>
    </div>

    <a href="/student/dashboard" class="back-link">⬅ Back to Dashboard</a>
</div>

</body>
</html>
